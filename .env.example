# Backend API server port
PORT=4000

# Absolute or relative path to the SQLite database file used by the API server.
# Defaults to ./data/apply-job.sqlite when unset.
DATABASE_PATH=./data/apply-job.sqlite

# Origin allowed to call the API (the Vite dev server).
CORS_ORIGIN=http://localhost:5173
